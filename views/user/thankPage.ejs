<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thank You</title>
    <style>
        .container {
            text-align: center;
            margin-top: 50px;
        }
        .button {
            margin: 20px;
            padding: 10px 20px;
            font-size: 16px;
            cursor: pointer;
        }
        .button-primary {
            background-color: #007f57;
            color: white;
            border: none;
            border-radius: 4px;
        }
        .button-secondary {
            background-color: #008CBA;
            color: white;
            border: none;
            border-radius: 4px;
        }
        .button-home {
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 4px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Thank You for Your Order!</h1>
        <p>Your order has been placed successfully.</p>
        <button class="button button-primary" onclick="window.location.href='/shop'">Continue Shopping</button>
        <% if (order) { %>
            <button class="button button-secondary" onclick="window.location.href='/order/<%= order._id %>'">Order Details</button>
        <% } else { %>
            <p>No recent orders found.</p>
        <% } %>
        <button class="button button-home" onclick="window.location.href='/'">Home</button>
    </div>
</body>
</html>
